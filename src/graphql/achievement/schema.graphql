type AchievementDone {
    title: String
    description: String
    createdAt: String
}

type Query {
    Achievements: [AchievementDone] @isPlayer
}

type Mutation {
    CompleteAchievement(title: String!): Boolean @isPlayer
}