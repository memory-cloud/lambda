scalar URL

type AchievementDone {
    title: String
    description: String
    completedAt: String
    image: URL
}

type Query {
    Achievements: [AchievementDone] @hasRole(requires: PLAYER)
}

type Mutation {
    CompleteAchievement(title: String!): Boolean @hasRole(requires: PLAYER)
}
